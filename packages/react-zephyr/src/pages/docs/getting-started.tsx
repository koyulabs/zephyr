import { ReactElement } from "react";
import "@twind/macro";
import DefaultLayout from "@layouts/Default";

export default function gettingStarted(): ReactElement {
  return (
    <DefaultLayout title="Getting Started">
      This should be getting started
    </DefaultLayout>
  );
}
